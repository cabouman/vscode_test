# VSCODE_test

This repository contains code and examples for learning how to use VS Code.

The image web app lives in `img-web-app/`.
Camera shake blur utilities and a Streamlit app live in `camera_model/`.
